namespace Snake
{
    internal class Program
    {
        private const int X = 60;
        private const int Y = 30;

        private static void Main(string[] args)
        {
            GameCore.Initialization(X, Y);
        }
    }
}
